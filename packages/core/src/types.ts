// Domain-split type definitions — see types/ subdirectory for individual modules.
// This barrel preserves backward compatibility for all `import ... from './types.js'` consumers.
export * from './types/index.js';
